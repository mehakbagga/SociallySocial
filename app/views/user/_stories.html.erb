<div class='row'>
  <% @stories.each do |story| %>
    <div class='col-2'>
      <%= link_to image_tag(story.image_url, size: '50x50', class: 'rounded-circle'), post_path(story.id) %>
    </div>
  <% end %>
</div>
